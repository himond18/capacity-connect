/* Capacity Connect — minimum submission flow bridge */
(function(){
 function boot(){
  const api=window.capacityAPI&&window.capacityAPI.api;if(!api)return false;
  const msg=m=>typeof toast==='function'?toast(m):alert(m),v=id=>(document.getElementById(id)||{}).value||'';
  function syncCourse(c){return c?{id:c.id,title:c.title||'Course',subject:c.subject||'',description:c.description||'',trainer:c.trainer||c.trainer_name||'',published:(c.status||'').toLowerCase()==='published',status:c.status||'draft',enrollmentCount:Number(c.enrollment_count||c.enrollmentCount||0),recordedClasses:[],notes:[],liveClasses:[]}:null}
  window.createCourse=async function(){const title=v('ctitle').trim(),subject=v('csubject').trim(),description=v('cdesc').trim();if(!title||!subject||!description)return msg('Fill course details.');try{const r=await api('/api/trainer/courses',{method:'POST',body:JSON.stringify({title,subject,description,status:'draft',is_free:true,price:0})});if(typeof db!=='undefined'&&r.course){db.courses=db.courses||[];db.courses.unshift(syncCourse(r.course));if(typeof save==='function')save()}msg('Course created as draft.');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.publishCourse=async function(id){try{const r=await api('/api/trainer/courses/'+encodeURIComponent(id),{method:'PATCH',body:JSON.stringify({status:'published'})});if(typeof db!=='undefined'){const c=db.courses.find(x=>String(x.id)===String(id));if(c)c.published=true;if(typeof save==='function')save()}msg('Course published.');if(typeof render==='function')render();return r}catch(e){msg(e.message)}};
  window.addRecordedClass=async function(){const course_id=v('recCourse'),title=v('recTitle').trim(),url=v('recUrl').trim(),description=v('recDesc').trim();if(!course_id||!title||!url)return msg('Course, title and video URL are required.');try{await api('/api/trainer/resources',{method:'POST',body:JSON.stringify({course_id,title,url,description,type:'recorded'})});msg('Recorded class added.');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.addCourseNote=async function(){const course_id=v('noteCourse'),title=v('noteTitle').trim(),url=v('noteUrl').trim(),description=v('noteDesc').trim();if(!course_id||!title||!url)return msg('Course, title and notes URL are required.');try{await api('/api/trainer/resources',{method:'POST',body:JSON.stringify({course_id,title,url,description,type:'notes'})});msg('Notes added.');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.scheduleLiveClass=async function(){const course_id=v('liveCourse'),title=v('liveTitle').trim(),date=v('liveDate'),time=v('liveTime'),link=v('liveLink').trim(),duration=v('liveDuration').trim(),description=v('liveDesc').trim();if(!course_id||!title||!date||!time||!link)return msg('Course, title, date, time and meeting link are required.');try{await api('/api/trainer/live-classes',{method:'POST',body:JSON.stringify({course_id,title,date,time,link,duration,description})});msg('Live class scheduled.');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.enroll=async function(id){try{const r=await api('/api/trainee/enrollments',{method:'POST',body:JSON.stringify({course_id:id})});if(typeof db!=='undefined'){const email=JSON.parse(sessionStorage.getItem('user')||'{}').email||'',u=db.users&&db.users.find(x=>String(x.email).toLowerCase()===email.toLowerCase());if(u){u.enrolled=u.enrolled||[];if(!u.enrolled.includes(id))u.enrolled.push(id);const c=db.courses&&db.courses.find(x=>String(x.id)===String(id));if(c)c.enrollmentCount=(c.enrollmentCount||0)+1;if(typeof save==='function')save()}}msg('Enrolled successfully.');if(typeof render==='function')render();return r}catch(e){msg(e.message)}};
  window.submitFeedback=async function(){const category=v('fcat'),message=v('fmsg').trim();if(!message)return msg('Enter your feedback.');try{await api('/api/feedback',{method:'POST',body:JSON.stringify({category,message})});msg('Feedback submitted.');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.approveUser=async function(id){try{await api('/api/admin/users/'+encodeURIComponent(id)+'/approval',{method:'PATCH',body:JSON.stringify({status:'approved'})});msg('User approved.');if(window.syncAdminStats)window.syncAdminStats();if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.submitAssessment=async function(id){const a=typeof db!=='undefined'&&db.assessments?db.assessments.find(x=>String(x.id)===String(id)):null;if(!a)return msg('Assessment not found.');let score=0,answers=[];for(let i=0;i<a.questions.length;i++){const x=document.querySelector('input[name="a'+a.id+'q'+i+'"]:checked');if(!x)return msg('Answer every question.');answers.push(Number(x.value));if(Number(x.value)===Number(a.questions[i].ans))score++}const pct=Math.round(score/a.questions.length*100);try{await api('/api/trainee/assessment-attempts',{method:'POST',body:JSON.stringify({assessment_id:id,answers,score:pct})});if(typeof db!=='undefined'){const u=typeof me==='function'?me():null;if(u){u.lastScore=pct;u.attempted=u.attempted||[];if(!u.attempted.includes(id))u.attempted.push(id);if(pct>=50)u.progress=Math.max(Number(u.progress||0),100)}}msg('Result: '+score+'/'+a.questions.length+' ('+pct+'%).');if(typeof render==='function')render()}catch(e){msg(e.message)}};
  window.issueCertificate=async function(courseId){try{const r=await api('/api/trainee/certifications',{method:'POST',body:JSON.stringify({course_id:courseId})});msg(r.alreadyIssued?'Certificate already issued.':'Certificate issued successfully.');return r}catch(e){msg(e.message)}};
  window.logout=function(){sessionStorage.clear();localStorage.removeItem('token');localStorage.removeItem('user');location.reload()};
  return true;
 }
 if(!boot())document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,800));
})();
